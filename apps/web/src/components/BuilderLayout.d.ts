import type { ReactNode } from "react";
export declare function BuilderLayout({ header, builder, inspector }: {
    header: ReactNode;
    builder: ReactNode;
    inspector: ReactNode;
}): import("react").JSX.Element;
